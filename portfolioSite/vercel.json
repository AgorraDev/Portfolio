
{
    "version": 2, 
    "builds": [
        {"src": "/api/send-email.cjs", "use": "@vercel/node"}
    ],
    "routes": 
        [{ 
        "src": "/api/send-email",
        "dest": "/api/send-email.cjs" 
        }],
    "buildCommand": "npm run build"
}