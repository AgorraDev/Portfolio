
{
    "version": 2, 
    "rewrites": 
        [{ 
        "source": "/(.*)",
        "destination": "/api" 
        }],
    "buildCommand": "npm run build",
    "engines": {
        "node": "18.x"
    }
}